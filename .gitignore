# ═══════════════════════════════════════════════════════════
# SECRETS — never commit these.
# Covers .env, .env.local, .env.production.local, .env.development.local
# ═══════════════════════════════════════════════════════════
.env
.env.*
!.env.example

# Certificates / keys
*.pem
*.key
*.p12
*.pfx

# ── Dependencies ──
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# ── Next.js build output ──
.next/
out/
build/
dist/
next-env.d.ts

# ── Vercel / deployment ──
.vercel/

# ── Testing / coverage ──
coverage/

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── TypeScript ──
*.tsbuildinfo

# ── Editors & OS junk ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# ── v0 sandbox internal files ──
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
